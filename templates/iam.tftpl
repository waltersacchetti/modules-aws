╔═════════════════════╗
║IAM Roles Information║
╚═════════════════════╝
${join("\n", [
for key, value in resource_map: (
  join("\n\t", [
    "(${key})${value.id}",
    "╚ Description: ${value.description}"
  ])
)
])}

